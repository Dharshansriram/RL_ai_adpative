# Assets

- **`wandb-openenv-pm-grpo.png`** — Summary plot (loss, GRPO mean reward, token count vs step) for the run **`openenv-pm-grpo`** in Weights & Biases. Regenerate with `python scripts/gen_wandb_summary_figure.py` if you change styling.
- Replace this file with a **screenshot export** from W&B if you want a pixel-perfect match to your dashboard.
